function albums(album) {
  const event=([])

  for( let i=0; i<album.length; i++) {
    event.push({
        title: `${album[i].albumname} 📀`,
        date: album[i].Releasedate,
        color: album[i].color,
        textColor: 'wihte'
    })
  }

  return event
}

export default albums